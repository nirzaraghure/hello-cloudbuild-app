# System Architecture

```mermaid
graph TD
    A[README] --> B[CONTRIBUTING]
    B --> C[ARCHITECTURE]
    C --> D[LICENSE]
    E[app.py] --> F[cloudbuild-delivery.yaml]
    F --> G[cloudbuild-trigger-cd.yaml]
    G --> H[cloudbuild.yaml]
    I[Dockerfile] --> J[kubernetes.yaml.tpl]
    K[app.py] --> L[test_app.py]
    M[app.py] --> N[known_hosts.github]
    O[cloudbuild-delivery.yaml] --> P[API Server]
    Q[API Server] --> R[Database]
    S[API Server] --> T[Frontend]
```

> Generated by GitGenius Sentinel