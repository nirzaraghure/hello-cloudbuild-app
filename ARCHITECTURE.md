# System Architecture

```mermaid
graph TD
    A[README] --> B[CONTRIBUTING]
    B --> C[ARCHITECTURE]
    C --> D[LICENSE]
    E[app.py] --> F[cloudbuild-delivery.yaml]
    F --> G[cloudbuild-trigger-cd.yaml]
    G --> H[cloudbuild.yaml]
    I[Dockerfile] --> J[kubernetes.yaml.tpl]
    K[test_app.py] --> L[app.py]
    M[known_hosts.github] --> N[cloudbuild-delivery.yaml]
    O[cloudbuild-delivery.yaml] --> P[cloudbuild-trigger-cd.yaml]
    Q[cloudbuild-trigger-cd.yaml] --> R[cloudbuild.yaml]
    S[cloudbuild.yaml] --> T[kubernetes.yaml.tpl]
    U[kubernetes.yaml.tpl] --> V[Dockerfile]
```

> Generated by GitGenius Sentinel