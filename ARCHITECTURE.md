# System Architecture

```mermaid
graph TD
    A[README] --> B[CONTRIBUTING]
    B --> C[ARCHITECTURE]
    C --> D[LICENSE]
    E[app.py] --> F[cloudbuild-delivery.yaml]
    F --> G[cloudbuild-trigger-cd.yaml]
    G --> H[cloudbuild.yaml]
    I[Dockerfile] --> J[kubernetes.yaml.tpl]
    K[test_app.py] --> L[app.py]
    M[known_hosts.github] --> N[cloudbuild.yaml]
    O[cloudbuild-delivery.yaml] --> P[cloudbuild-trigger-cd.yaml]
    Q[cloudbuild-trigger-cd.yaml] --> R[cloudbuild-delivery.yaml]
    S[cloudbuild.yaml] --> T[cloudbuild-delivery.yaml]
    U[cloudbuild-delivery.yaml] --> V[cloudbuild-trigger-cd.yaml]
    W[cloudbuild-trigger-cd.yaml] --> X[cloudbuild-delivery.yaml]
    Y[cloudbuild.yaml] --> Z[cloudbuild-delivery.yaml]
```

> Generated by AppGenius Sentinel